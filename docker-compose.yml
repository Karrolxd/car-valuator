services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: car_valuator
      POSTGRES_USER: car_valuator
      POSTGRES_PASSWORD: car_valuator
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: